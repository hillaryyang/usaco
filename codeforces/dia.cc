//http://www.usaco.org/index.php?page=viewproblem2&cpid=643
#include <iostream>
#define ll long long
using namespace std;

int main() {
    int n, k, x; cin >> n >> k;
    vector<int> a;

    for (int i = 0; i < n; i++) {
        cin >> x; a.push_back(x);
    }

    sort(a.begin(), a.end());

    
}